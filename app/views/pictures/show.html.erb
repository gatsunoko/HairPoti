<div id="modal_picture_show">
  <% if controller.action_name != 'show' %>
    <%= render 'show_modal' %>
  <% else %>
    <%= render 'show_modal_picture_area', picture: @picture %>
  <% end -%>
</div>