<div class="card1_area">
  <%= link_to(image_tag(@picture1.url + 'media/?size=l', class: 'card1'), conflict_elo_path(win: @picture1.id, lose: @picture2.id), remote: true) %>
</div>
<div class="card2_area">
  <%= link_to(image_tag(@picture2.url + 'media/?size=l', class: 'card2'), conflict_elo_path(win: @picture2.id, lose: @picture1.id), remote: true) %>
</div>