<% if @resource.unconfirmed_email %>
-----------------------------
このメールは自動的に送信されています。返信はできません。
-----------------------------
━━━━━━━━━━━━━
◆「へあぽち」メールアドレス変更のご連絡◆
━━━━━━━━━━━━━

平素より「へあぽち」をご利用いただき、ありがとうございます。
ご登録されているメールアドレスの情報が更新されました。

「へあぽち」ではメールアドレスを用いた認証をお願いしております。
お手数ですが以下のリンクをクリックして、「変更」手続きを完了してください。

<%= url_for(confirmation_url(@resource, confirmation_token: @token)) %>

このメールに心当たりが無い場合は、無視してください。

<% else %>
-----------------------------
このメールは自動的に送信されています。返信はできません。
-----------------------------
━━━━━━━━━━━━━
◆「へあぽち」無料会員登録のご案内◆
━━━━━━━━━━━━━

このたびは「へあぽち」無料会員へご登録いただきありがとうございます。
「へあぽち」では本登録時にメールアドレスの認証をお願いしております。

お手数ですが下記リンクをクリックして、ユーザー本登録を完了してください。

<%= url_for(confirmation_url(@resource, confirmation_token: @token)) %>

このメールに心当たりが無い場合は、無視してください。

<% end %>
